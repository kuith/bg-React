/* eslint-disable no-unused-vars */
import "@testing-library/jest-dom"; // Extiende expect con funciones como toBeInTheDocument
import { expect } from "vitest"; // Esto asegura que `expect` esté disponible
